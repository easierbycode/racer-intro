node_modules
dist
# Fresh build output (app/)
_fresh
# recorder / transcode output
out
# UI bundle built into the Fresh app by `deno task ui:build`
app/static/ui
# saved Instagram device/session state (contains auth tokens)
.ig-state.json
